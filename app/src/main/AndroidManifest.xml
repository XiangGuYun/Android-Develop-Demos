<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".huitu.HuiTuActivity" />
        <activity android:name=".huitu.BaseKnowledgeActivity" />
        <activity android:name=".huitu.PaintActivity" />
        <activity android:name=".huitu.XfermodeActivity" />
        <activity android:name=".huitu.DrawTextActivity">

        </activity>
        <activity android:name=".ImageViewerActivity"></activity>
    </application>

</manifest>